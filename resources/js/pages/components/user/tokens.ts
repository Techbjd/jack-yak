// Co-located user tokens — card shell shared verbatim by all four user
// cards (ProfileCard, SavedCard, SettingsCard, SignOutCard), composed per
// card with cn() so theme.ts stays untouched.

/** User card shell — shadow, radius, padding shared by every user card */
export const userCard =
    'shadow-card w-full rounded-xl bg-white p-5 md:rounded-none md:p-10';
